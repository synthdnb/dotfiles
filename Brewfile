tap "loft-sh/tap", trusted: true
tap "raine/workmux", trusted: true

# === Tools (brew resolves and installs their dependencies automatically) ===
# Tool for generating GNU Standards-compliant Makefiles
brew "automake"
# Command-line fuzzy finder written in Go
brew "fzf"
# GitHub command-line tool
brew "gh"
# Distributed revision control system
brew "git"
# Kubernetes package manager
brew "helm"
# Improved top (interactive process viewer)
brew "htop"
# Lightweight and flexible command-line JSON processor
brew "jq"
# Tool that can switch between kubectl contexts easily and create aliases
brew "kubectx"
# Ambitious Vim-fork focused on extensibility and agility
brew "neovim"
# Development kit for the Java programming language
brew "openjdk@11"
# Reattach process (e.g., tmux) to background
brew "reattach-to-user-namespace"
# Search tool like grep and The Silver Searcher
brew "ripgrep"
# Safe, concurrent, practical language
brew "rust"
# Static analysis and lint tool, for (ba)sh scripts
brew "shellcheck"
# Terminal multiplexer
brew "tmux"
# Creates fully functional virtual k8s cluster inside host k8s cluster's namespace
brew "loft-sh/tap/vcluster", trusted: true
# Executes a program periodically, showing output fullscreen
brew "watch"
# Internet file retriever
brew "wget"
# Access X11 clipboards from the command-line
brew "xclip"
# Smarter cd command, inspired by z and autojump
brew "zoxide"
# UNIX shell (command interpreter)
brew "zsh"

# Command-line interface for 1Password
cask "1password-cli"
# Terminal emulator that uses platform-native UI and GPU acceleration
cask "ghostty"
# Alternative input source switcher
cask "kawa-app"
# Open-source cross-platform alternative to AirDrop
cask "localsend"
# Reverse proxy, secure introspectable tunnels to localhost
cask "ngrok"

# --- added by hand: referenced in configs but not auto-captured by `brew bundle dump` ---
# (a later dump from a fully set-up machine will recapture these)
brew "mise"
brew "raine/workmux/workmux"
cask "font-meslo-lg-nerd-font"
cask "orbstack"
